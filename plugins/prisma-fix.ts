// This plugin helps fix Prisma integration issues with Nuxt/Nitro
import { defineNuxtPlugin } from '#app'

export default defineNuxtPlugin(() => {
  // This is just a plugin to ensure Prisma is properly initialized
  // No actual code needed here - the real fix is in the Nuxt config
})